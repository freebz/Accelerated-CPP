vector<string> split(const string& str) {
    vector<string> ret;
    // str을 단어들로 구분하여 ret에 저장

    return ret;
}


template <class T> class Vec {
public:
    ~Vec() { uncreate(); }
    // 이전과 같음.
};
